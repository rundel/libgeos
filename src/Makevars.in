PKG_CPPFLAGS=@PKG_CPPFLAGS@
PKG_LIBS=@PKG_LIBS@
GEOS_CONFIG=@GEOS_CONFIG@

.PHONY: all flags

all: $(SHLIB) flags

flags:
	if test "${GEOS_CONFIG}" != "no"; then									\
		printf "${GEOS_CONFIG}\n" > ../inst/geos_config_path;	\
	fi
